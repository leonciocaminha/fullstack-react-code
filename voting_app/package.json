{
  "name": "voting_app",
  "version": "1.0.0",
  "author": "Fullstack.io",
  "scripts": {
    "go": "open http://localhost:3000; npm run server",
    "e2e": "nightwatch",
    "test": "./node_modules/.bin/concurrently -k 'npm run server' 'npm run e2e'",
    "server": "./node_modules/http-server/bin/http-server . -p 3000"
  },
  "private": true,
  "dependencies": {
    "http-server": "0.8.5"
  },
  "devDependencies": {
    "concurrently": "2.2.0"
  }
}
